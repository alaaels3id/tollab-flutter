# سجل التغييرات (CHANGELOG)

جميع التغييرات والتحسينات الملحوظة في تطبيق **طُلاّب (Tollab)** موثقة في هذا الملف.

---

## [1.0.1] - 2026-09-11

### ✨ الميزات الجديدة (New Features)
- **صفحات مخصصة لإضافة وتعديل البيانات (Dedicated Form Screens)**:
  - تحويل نوافذ إضافة الطلاب إلى شاشة كاملة مخصصة [`StudentFormScreen`](lib/features/students/presentation/student_form_screen.dart).
  - تحويل نوافذ إضافة المجموعات إلى شاشة كاملة مخصصة [`GroupFormScreen`](lib/features/groups/presentation/group_form_screen.dart).
- **الحذف الإجباري المتسلسل (Force Delete)**:
  - إمكانية الحذف الإجباري للطلاب مع حذف سجلات الاشتراكات المرتبطة بهم تلقائياً.
  - إمكانية الحذف الإجباري للمجموعات مع حذف كافة سجلات الاشتراكات، وسجلات التنبيهات، والطلاب التابعين للمجموعة.
- **شريط تنقل سفلي متوافق مع كافة المنصات (Adaptive Bottom Navigation)**:
  - دعم أصلي لنظام iOS باستخدام `CNTabBar` مع رموز Apple SF Symbols.
  - شريط تنقل Material 3 مخصص لنظام Android مع أيقونات Google الرسمية بدقة عالية.
- **إشعارات Firebase السحابية (Firebase Cloud Messaging - FCM)**:
  - دمج وإعداد `firebase_core` و `firebase_messaging`.
  - إضافة زر في شاشة الإعدادات لنسخ رمز الجهاز (`FCM Token`) بسهولة لتجربة الإشعارات من Firebase Console.
  - تفعيل ظهور الإشعارات في الواجهة الأمامية (Foreground Notifications) على iOS.

### 🎨 تحسينات واجهة المستخدم (UI/UX Improvements)
- توسيط كافة عناوين الشاشات العلوية (`centerTitle: true`) لتوحيد هوية التطبيق عبر كافة الصفحات.
- تحسين رسائل التأكيد ونوافذ التحذير عند الحذف أو استعادة النسخ الاحتياطية.
- تحسين التوافقية الكاملة مع اتجاه الكتابة من اليمين لليسار (RTL).

### ⚙️ تحسينات البناء والأداء (Build & Infrastructure)
- ترقية إصدار Gradle إلى `9.3.1` ليتوافق مع Java 25 ومكون Android Gradle Plugin `9.1.0`.
- تفعيل خاصية `coreLibraryDesugaring` لدعم مكتبة التنبيهات المحلية على كافة إصدارات الأندرويد.
- إضافة تصريحات `UIBackgroundModes` و `UNUserNotificationCenterDelegate` في iOS لدعم إشعارات النظام.
- إعداد ملف `.gitignore` شامل لحماية مفاتيح التوقيع واستبعاد كافة ملفات ومجلدات البناء المؤقتة.

---

## [1.0.0] - الإصدار الأولي (Initial Release)
- إدارة المجموعات والاشتراكات الشهرية للطلاب (Local-First).
- لوحة تحكم إحصائية تفاعلية.
- سجل وأرشيف للمدفوعات السابقة.
- نظام النسخ الاحتياطي واستعادة قاعدة البيانات محلياً.
